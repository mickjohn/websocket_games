class Player {
    username: string;
    active: boolean;

    constructor(username: string, active: boolean) {
        this.username = username;
        this.active = active;
    }
}

export default Player;